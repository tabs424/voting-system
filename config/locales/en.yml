en:
  pundit:
#    default: You are not authorized to perform this action
    user: # User policy
      sessions_policy:
        create?: Invalid Credentials
    admin: # Admin policy
      sessions_policy:
        create?: Invalid Credentials
